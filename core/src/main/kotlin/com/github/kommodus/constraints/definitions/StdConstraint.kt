package com.github.kommodus.constraints.definitions

/**
 * Marker interface to keep track of all library provided definitions at compile time
 * e.g. providing your own messages per constraint
 */
sealed interface StdConstraint